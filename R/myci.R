#' Title
#'
#' @param x = rnorm(sizeSample, mean, sd)
#'
#' @return a range for the sample to be in the 95% range
#' @export
#'
#' @examples myCi(x)
#' Had to block this out cause it caused errors with my check cause x is a complicated variable
#myCi <- function(x){
 # mp = c(-1,1)

  #ci_95 = mean(x) + mp*qt(0.95,30) * sd(x)/sqrt(31)
  #ci_95
#}
